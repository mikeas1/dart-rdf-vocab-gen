name: rdf_vocab_gen
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0

dependencies:
  source_gen: ^1.2.1
  rdf: ^1.0.0
  build: ^2.2.1
  quiver: ^3.0.1+1
  http: ^0.13.4

dependency_overrides:
  rdf:
    path: /home/mike/projects/dart-rdf
